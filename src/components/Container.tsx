import styled from "@emotion/styled/macro";
import tw from "twin.macro";

export const ContainerFlex = styled.div([
  tw`container mx-auto flex px-5 md:flex-row flex-col items-center`,
]);

export const Container = styled.div([tw`container px-5 py-24 mx-auto`]);
